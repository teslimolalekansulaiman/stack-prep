[project]
name = "score-pilot-engine"
version = "0.1.0"
description = "Score Pilot learning engine: pure decision rules, no I/O"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/engine"]
